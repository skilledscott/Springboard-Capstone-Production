# Decide on which model to use considering [size, runtime, COCO mAP performance]
# Load a pretrained model [tensorflow, roboflow, pytorch, hugging face]
# Insert transfer learning step (doesn't need to improve performance much, but could show a before/after mAP)
# Save model to onnx file
